---
interface Props {
  start: string;
  end: string;
}

const { start, end } = Astro.props;
---

<div class="flex items-center gap-2">
  <span class="text-content-xs text-gray-500 dark:text-gray-400">
    {start}{end !== '' && ` - ${end}`}
  </span>
</div>
