---
import { getCollection } from 'astro:content';
import FormattedDate from '../../components/FormattedDate.astro';
import DefaultLayout from '../../layouts/DefaultLayout.astro';

const posts = (await getCollection('projects')).sort(
  (a, b) => b.data.pubDate.valueOf() - a.data.pubDate.valueOf(),
);
---

<DefaultLayout title="Projects" description="all my projects">
	<section class="p-4">
		<ul class="flex flex-wrap gap-[2rem] list-none m-0 p-0">
			{
				posts.map((post, index) => (
					<li class={`${index === 0 ? 'w-full mb-[1rem] text-center' : 'w-[calc(50%-1rem)]'}`}>
						<a href={`/projects/${post.slug}/`} class="block no-underline transition duration-200 ease-in-out">
							<h4 class={`title m-0 ${index === 0 ? 'text-[2.369rem] leading-10' : 'leading-5'}`}>{post.data.title}</h4>
							<div class="inline-block w-full h-[1px] my-2" style={`background: ${post.data.color};`}></div>
							<p class="date m-0 text-gray-500">
								<FormattedDate date={post.data.pubDate} />
							</p>
							<div class="color-block" style={`background: ${post.data.color};`}></div>
						</a>
					</li>
				))
			}
		</ul>
	</section>
</DefaultLayout>
