---
interface Props {
  start: string;
  end: string;
}

const { start, end } = Astro.props;
---

<div class="flex items-center gap-2 mb-2">
  <span class="text-[0.8125rem] font-medium text-gray-500 dark:text-gray-400 tracking-wide">
    {start} - {end}
  </span>
</div>
