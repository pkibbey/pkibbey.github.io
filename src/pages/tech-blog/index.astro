---
import { getCollection } from 'astro:content';
import PostCard from '../../components/PostCard.astro';
import DefaultLayout from '../../layouts/DefaultLayout.astro';

const posts = (await getCollection('blog')).sort(
  (a, b) => b.data.pubDate.valueOf() - a.data.pubDate.valueOf(),
);

const [featuredPost, ...otherPosts] = posts;
---

<DefaultLayout 
  title="Tech Blog" 
  description="Articles about software development, tech, and my experiences"
>
  <section class="max-w-7xl mx-auto">
    <div class="mb-16">
      <PostCard post={featuredPost} baseUrl="/tech-blog/" variant="featured" />
    </div>
    <div class="mb-8">
      <h2 class="text-lg font-semibold mb-8 text-gray-900 dark:text-gray-100">Recent Entries</h2>
      <ul class="grid sm:grid-cols-2 lg:grid-cols-3 gap-x-8 gap-y-12 list-none m-0 p-0">
        {otherPosts.map((post) => (
          <li>
            <PostCard post={post} baseUrl="/tech-blog/" variant="default" />
          </li>
        ))}
      </ul>
    </div>
  </section>
</DefaultLayout>
